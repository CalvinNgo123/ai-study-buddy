apiVersion: v1
kind: Service
metadata:
  name: study-guide-backend
  namespace: study-guide
  labels:
    app: study-guide-backend
spec:
  selector:
    app: study-guide-backend
  ports:
  - port: 8000
    targetPort: 8000
    name: http
  type: ClusterIP
