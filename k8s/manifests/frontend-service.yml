apiVersion: v1
kind: Service
metadata:
  name: study-guide-frontend
  namespace: study-guide
  labels:
    app: study-guide-frontend
spec:
  selector:
    app: study-guide-frontend
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30080
    name: http
  type: NodePort
